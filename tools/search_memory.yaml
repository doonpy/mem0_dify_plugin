identity:
  name: search_memory
  author: beersoccer
  label:
    en_US: Search Memory
    zh_Hans: 搜索记忆
description:
  human:
    en_US: Retrieve relevant memories from Mem0 using powerful semantic and filtered search capabilities
    zh_Hans: 使用强大的语义检索和筛选功能，从 Mem0 检索相关记忆
  llm: Use this tool to search and retrieve relevant memories from Mem0 using semantic search with optional filters.
parameters:
  - name: query
    type: string
    required: true
    label:
      en_US: Query string
      zh_Hans: 查询语句
    human_description:
      en_US: The query string to search for specific memories.
      zh_Hans: 用于搜索特定记忆的查询语句。
    llm_description: The query string to search for specific memories.
    form: llm
  - name: user_id
    type: string
    required: true
    label:
      en_US: User ID
      zh_Hans: 用户ID
    human_description:
      en_US: Required user identifier for scoping the search.
      zh_Hans: 必填，用于限定搜索范围的用户标识符。
    llm_description: Required user identifier for scoping the search.
    form: llm
  - name: agent_id
    type: string
    required: false
    label:
      en_US: Agent ID
      zh_Hans: 代理ID
    human_description:
      en_US: Optional agent identifier. Only provide if you need to filter by a specific agent. Leave empty otherwise.
      zh_Hans: 可选的代理标识符。仅在需要按特定代理过滤时提供。否则留空。
    llm_description: Optional agent identifier. Only provide if filtering by a specific agent is needed. Leave empty otherwise.
    form: llm
  - name: run_id
    type: string
    required: false
    label:
      en_US: Run ID
      zh_Hans: 运行ID
    human_description:
      en_US: "Optional unique identifier for tracking the entire memory operation call chain. Recommended to use Dify's workflow_run_id to link multiple memory operations in the same workflow. Note: This parameter is only for tracing the call chain and is NOT used as a condition for memory layering or filtering."
      zh_Hans: 可选的唯一标识符，用于追踪整个记忆操作调用链。推荐使用 Dify 的 workflow_run_id 来关联同一工作流中的多个记忆操作。注意：此参数仅用于追踪调用链，不作为记忆分层或过滤的条件。
    llm_description: "Optional unique identifier for tracking the entire memory operation call chain. Recommended to use Dify's workflow_run_id to link multiple memory operations in the same workflow. This parameter is only for tracing and is NOT used as a condition for memory layering or filtering."
    form: llm
  - name: filters
    type: string
    required: false
    label:
      en_US: Advanced Filters (JSON)
      zh_Hans: 高级过滤器（JSON）
    human_description:
      en_US: Optional advanced filters in JSON with AND/OR logic. Only provide if complex filtering is needed. Leave empty otherwise.
      zh_Hans: 可选的高级过滤器，JSON 格式，支持 AND/OR 逻辑。仅在需要复杂过滤时提供。否则留空。
    llm_description: Optional advanced filters in JSON with AND/OR logic. Only provide if complex filtering is needed. Leave empty otherwise.
    form: llm
  - name: top_k
    type: number
    required: false
    default: 5
    label:
      en_US: Top K
      zh_Hans: Top K
    human_description:
      en_US: Optional maximum number of results to return. Defaults to 5 if not specified.
      zh_Hans: 可选，返回的最大结果数量。未指定时默认为 5。
    llm_description: Optional maximum number of results to return. Defaults to 5 if not specified.
    form: llm
  - name: score_threshold
    type: number
    required: false
    label:
      en_US: Score Threshold
      zh_Hans: 分数阈值
    human_description:
      en_US: Optional minimum similarity score (0.0–1.0). Results below this score are filtered out. Overrides the provider-level default. Leave empty to use the provider default (or no threshold if unset).
      zh_Hans: 可选的最低相似度分数（0.0–1.0）。分数低于此值的结果将被过滤。会覆盖 Provider 层的默认值。留空则使用 Provider 默认值（未设置时表示不使用阈值）。
    llm_description: Optional minimum similarity score (0.0–1.0). Results below this score are filtered out. Leave empty to use the provider-configured default.
    form: llm
  - name: timeout
    type: number
    required: false
    label:
      en_US: Timeout (seconds)
      zh_Hans: 超时时间（秒）
    human_description:
      en_US: Optional timeout in seconds for the search operation (async mode only). Defaults to 15 if not specified. Only applies when async_mode=true.
      zh_Hans: 可选，搜索操作的超时时间（秒，仅异步模式）。未指定时默认为 15 秒。仅在 async_mode=true 时生效。
    llm_description: Optional timeout in seconds for the search operation (async mode only). Defaults to 15 if not specified. Only applies when async_mode=true.
    form: form
extra:
  python:
    source: tools/search_memory.py
